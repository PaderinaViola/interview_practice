#You are given a string s consisting of words separated by spaces, and an integer k. For each word in the string, you must rotate its characters to the right by k positions.
#Return the modified string.
s = "Hello World"
k = 1
def f(s,k):
    s_new = s.split()
    return s_new

print(f(s,k))